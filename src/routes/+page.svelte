<script lang="ts">
  // let error = $state();
  // function createScene(canvas: HTMLCanvasElement) {
  //   const gl = canvas.getContext("webgl2");
  //   if (!gl) {
  //     error = "This browser does not support WebGL 2";
  //     return;
  //   }
  // }
</script>

<!-- {#if error}
  <p>{error}</p>
  {:else}
  <canvas {@attach createScene}>Simple triangle</canvas>
{/if} -->
<main class="flex min-h-screen items-center justify-center">Graphics with WebGL</main>
